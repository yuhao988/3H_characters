import { Link } from "react-router-dom";

export default function CharacterDetail() {
  return (
    <div>
      <h1>Character: placeholder for name</h1>
      
      <Link to="/characters">Back to characters page</Link>
    </div>
  );
}